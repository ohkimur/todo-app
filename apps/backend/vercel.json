{
  "version": 2,
  "buildCommand": "cd ../.. && pnpm build:backend",
  "outputDirectory": "dist",
  "devCommand": "pnpm dev",
  "builds": [
    {
      "src": "src/index.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "src/index.ts"
    }
  ]
}
